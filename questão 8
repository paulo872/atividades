let positivos = 0;

for (let i = 1; i <= 5; i++) {
   let numero = parseFloat(prompt("Digite o " + i + "º número:"));
  
   if (numero > 0) {
       positivos++;
   }
}

alert("Quantidade de números positivos: " + positivos);

